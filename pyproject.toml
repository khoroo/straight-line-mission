[tool.poetry]
name = "straight-line-mission"
version = "0.1.0"
description = ""
authors = ["Rob Sparks"]

[tool.poetry.dependencies]
python = "3.8.12"
rioxarray = "^0.9.1"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
